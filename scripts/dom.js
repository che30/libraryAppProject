export default function Book(title, author, pages, id, status, isbn) {
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.id = id;
  this.status = status;
  this.isbn = isbn;
}
